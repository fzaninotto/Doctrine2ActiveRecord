{% block ArrayAccess %}

    /**
     * Set a property of the entity by name.
     *
     * @param string $offset The property name.
     * @param mixed  $value  The value.
     *
     * @see setByName()
     */
    public function offsetSet($offset, $value)
    {
        return $this->setByName($offset, $value);
    }

    /**
     * Get a property of the entity by name.
     *
     * @param string $offset The property name.
     *
     * @return mixed Some data.
     *
     * @see getByName()
     */
    public function offsetGet($offset)
    {
        return $this->getByName($offset);
    }

    /**
     * ArrayAccess implementation.
     *
     * @throws \LogicException because you cannot check if data exists.
     */
    public function offsetExists($offset)
    {
        throw new \LogicException('You cannot check if data exists in the entity.');
    }

    /**
     * ArrayAccess implementation.
     *
     * @throws \LogicException because you cannot unset data in the entity.
     */
    public function offsetUnset($offset)
    {
        throw new \LogicException('You cannot unset data in the entity.');
    }
{% endblock %}